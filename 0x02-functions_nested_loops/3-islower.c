#include "main.h"

/**
 * _islower - Checks for lowercase character
 * @c: Thexter to be checked
 * Return: 1 for lowercase xters or 0 anyhting for anyhting else.
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}

