#include "main.h"

/**
 * mul - multiplies two integers.
 * @a - first number to multiply
 * @b - number to multiply with the second
 * Return: the result of the multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
